export interface PageContent {
    pageTitle: string;
    header: string;
    mainContent: string;
    footer: string;
    headerHTML: string;
    mainContentHTML: string;
    footerHTML: string;
    css: string;
}